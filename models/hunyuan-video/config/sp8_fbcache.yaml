model_args:
  prompt: "A cat walks on the grass, realistic style."
  video-size: [720, 1280]            # [H, W]
  video-length: 129                  # 4k+1 form
  infer-steps: 50                    # denoising steps
  seed: 42
  embedded-cfg-scale: 6.0
  flow-shift: 7.0
  flow-reverse: true
  ulysses-degree: 8                  # Ulysses SP degree
  ring-degree: 1                     # Ring Attention SP degree
  use-vae-parallel: true

model_name: "hunyuan-video"
world_size: 8                        # must equal ulysses-degree * ring-degree
master_port: 29600
entry_script: "sample_video.py"

dit_cache:
  method: "FBCache"
  enable_separate_cfg: true
  params:
    rel_l1_thresh: 0.05              # L1 diff threshold
